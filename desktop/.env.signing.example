# Optional Windows Authenticode signing for the NSIS installer.
# Copy to desktop/.env.signing and set values before building on Windows.
#
# CSC_LINK=path/to/certificate.pfx
# CSC_KEY_PASSWORD=your-pfx-password
